# Convert a given string into lowercase letters.

class Solution:
    def toLower(self, s: str) -> str:
        # Convert the string to lowercase using Python's built-in method
        convert = s.lower()
        # Return the converted string
        return convert
